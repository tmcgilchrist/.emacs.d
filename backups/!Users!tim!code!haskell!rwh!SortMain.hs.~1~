module Main where

import Data.Time.Clock (diffUTCTime, getCurrentTime)
import System.Environment (getArgs)
import System.Random (StdGen, getStdGen, random)

import Sorting

-- testFunction = sort
-- testFunction = seqsort
testFunction = parSort
-- testFunction = parSort2 s