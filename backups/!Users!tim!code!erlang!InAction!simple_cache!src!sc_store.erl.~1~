-module(sc_store).

-export([
         init/0,
         insert/2,
         delete/1,
         lookup
        ]).

-define(TABLE_ID, ?MODULE).

init() ->
    ets:new(?TABLE_ID, [public, named_table]),
    ok.
