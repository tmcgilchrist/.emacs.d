require 'spec_helper'

describe Link do
    it 'should save given the correct attributes' do
       
    end

    it 'should require an original_url'

    it 'should generate a unique short_url'

    it 'should map same url to same short_url'

    it 'should map different urls to different short_urls'

end
