    /* This sequence of instructions will encrypt one character.
     *
     * li $r4 0x00000      # Base address of lookup table
     * li $r5 0x000F8      # Base address of plaintext
     * li $r8 0x0F000      # Address of Encryption Key
     * li $r9 0xF0000       # Address of Cyphertext
     *
     * li $r7 0x00000            # Counter for loop
     * lw $r1 $r7($r5)       # load first 8 bit character $r1: INPUT()
     *
     * C:
     * lw $r0 $r7($r8)       # load 8 bits of key $r0: KEY()
     * xor $r3 $r1 $r2       # <op> <src1> <src2> <dest>: 
     * lw $r1 $r3($r4)       # load value from lookup table: OUTPUT()
     * add $r7 0x01 $r7     # increment loop
     * bne $r7 0x07 C       # Jump to beginning of block
     *
     * sw $r9 $r0           # store word in result address
     */
So from that sequence we'll need to following instructions:

li <dest> <imm> 
